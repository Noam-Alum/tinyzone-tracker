remove the online tag. on mobile show each website in one row (Or in horizonatal screens sizes) make the domain the title and the website title smaller under it.

Add an endpoint that sends user to one of the websites available under /go and add a button that users can click on
